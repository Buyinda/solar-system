name: My first workflow

on: push
jobs: 
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: welcome message
              run: echo "my first github actions"

            - name: list files
              run: ls

            - name: read files
              run: cat README.md